---
# generated by https://github.com/magodo/terraform-plugin-framework-docs
page_title: "resolve_map Function - terraform-provider-rest"
subcategory: ""
description: |-
  Resolve all ref: expressions in a map of resource instances
---

# resolve_map (Function)

Takes a resolution context and a map of resource instances (e.g. the raw
storage_accounts map from YAML). Walks every string value in each instance;
if it starts with "ref:", resolves it against the context. List elements
with "ref:" prefixes are also resolved. Non-ref values pass through unchanged.
Returns the fully-resolved map with the same structure.

## Signature

```text
resolve_map(context Dynamic,resources Dynamic) Dynamic
```

## Arguments

1. `context` (Dynamic, Unknownable) The resolution context map (merged module outputs + input config).
1. `resources` (Dynamic, Unknownable) Map of resource instances to resolve, e.g. { mysa = { location = "ref:resource_groups.rg1.location", sku = "Standard_LRS" } }.
